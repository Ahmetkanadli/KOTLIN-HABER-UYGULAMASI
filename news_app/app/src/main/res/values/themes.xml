<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.News_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.News_app" parent="Base.Theme.News_app" />

    <style name="BottomNavigationViewStyle" parent="Widget.Design.BottomNavigationView">
        <item name="android:background">@color/white</item> <!-- Önerilen: BottomNavigationView'ın arka plan rengi -->
    </style>

    <style name="BottomNavigationView.ActiveTextAppearance" parent="@style/TextAppearance.AppCompat.Small">
        <item name="android:textColor">@android:color/black</item> <!-- Aktif metin rengi -->
    </style>

    <style name="BottomNavigationView.InactiveTextAppearance" parent="@style/TextAppearance.AppCompat.Small">
        <item name="android:textColor">@android:color/black</item> <!-- Pasif metin rengi -->
    </style>


</resources>